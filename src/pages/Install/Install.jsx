import React from 'react';
import PropTypes from 'prop-types';

const Install = () => {
    return (
        <div>
            Install page
        </div>
    );
};

Install.propTypes = {

};

export default Install;